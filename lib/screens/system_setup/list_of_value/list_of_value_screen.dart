import 'package:flutter/material.dart';

class ListOfValueScreen extends StatelessWidget {
  const ListOfValueScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}